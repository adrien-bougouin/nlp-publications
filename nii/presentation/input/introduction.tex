\section*{Introduction}
  \begin{frame}{Introduction}
    \framesubtitle{Problem statement}

    \uncover<+->{
      \begin{block}{Keyphrases}
        \begin{itemize}
          \item{Word or multi-word expressions}
          \item{\textbf{Overview} of the content of a document}
        \end{itemize}
      \end{block}
    }

    \uncover<+->{
      \begin{exampleblock}{Applications}
          \begin{multicols}{2}
          \begin{itemize}
            \item{Document indexing}
            \item{Document clusterng}
            \item{Text summarization}
            \item{Query expansion}
            \item{Targeted advertising}
            \item{etc.}
          \end{itemize}
        \end{multicols}
      \end{exampleblock}
    }

    \uncover<+->{
      \begin{alertblock}{But\dots}
        Many documents do not have associated keyphrases.
      \end{alertblock}
    }
  \end{frame}

  \begin{frame}{Introduction}
    \framesubtitle{Automatic keyphrase extraction}

    \begin{figure}
      \centering
      \begin{tikzpicture}[thin,
                          align=center,
                          scale=.3,
                          node distance=1.5cm,
                          every node/.style={text centered, transform shape}]
        \node[io] (document) {document};
        \node[component] (preprocessing) [below=of document] {Preprocessing};
        \node[component] (candidate_extraction) [below=of preprocessing] {Candidate Selection};
        %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
        \uncover<3->{
          \node[component] (preprocessing2) [right=of preprocessing] {Preprocessing};
          \node[io] (training_documents) [above=of preprocessing2] {training documents};
          \node[component] (candidate_extraction2) [below=of preprocessing2] {Candidate Selection};
          \node[component] (training) [below=of candidate_extraction2] {Training};
          \node[io] (model) [below=of training] {classification model};
          \node[component] (candidate_classification) [below=of model, yshift=-1cm] {Candidate Classification};
          \node[above=of training_documents, yshift=-1.25cm] (training_label) {\huge training};
        }
        %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
        \node[component] (candidate_ranking) [left=of candidate_classification] {Candidate Ranking};
        \node[component, minimum width=19.5cm, xshift=5.25cm] (keyphrase_selection) [below=of candidate_ranking] {Keyphrase Selection};
        \node[io] (keyphrases) [below=of keyphrase_selection] {keyphrases};

        \path[->, thick] (document) edge (preprocessing);
        \path[->, thick] (preprocessing) edge (candidate_extraction);
        \uncover<2->{
          \path[->, thick] (candidate_extraction) edge node [rotate=-90, xshift=-.5em, anchor=south] {\huge unsupervised} (candidate_ranking);
        }
        \visible<1>{
          \path[->, thick] (candidate_extraction) edge (candidate_ranking);
        }
        %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
        \uncover<3->{
          \path[->, thick] (training_documents) edge (preprocessing2);
          \path[->, thick] (preprocessing2) edge (candidate_extraction2);
          \path[->, thick] (candidate_extraction2) edge (training);
          \path[->, thick] (training) edge (model);
          \path[->, thick] (candidate_extraction) edge node [yshift=.5em, rotate=-43, anchor=south east] {\huge supervised} (candidate_classification);
          \path[->, thick] (model) edge (candidate_classification);
          \path[->, thick] (candidate_classification) edge (keyphrase_selection);
        }
        %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
        \path[->, thick] (candidate_ranking) edge (keyphrase_selection);
        \path[->, thick] (keyphrase_selection) edge (keyphrases);

        \begin{pgfonlayer}{background}
          \uncover<3->{
            \filldraw[line width=2mm,join=round,black!10] (training_documents.north -| preprocessing2.east) rectangle (model.south  -| preprocessing2.west);
          }
        \end{pgfonlayer}
      \end{tikzpicture}
    \end{figure}
  \end{frame}

  \begin{frame}{Introduction}
    \framesubtitle{Our work}

    Combining unsupervised and supervised methods:
    \begin{enumerate}
      \item{Clustering candidate keyphrases into topics}
      \item{Ranking topics \textbf{in an unsupervised way}}
      \item{Selecting keyphrases from the bes topics \textbf{in a supervised
            way}}
    \end{enumerate}
  \end{frame}

