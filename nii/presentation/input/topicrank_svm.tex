\section{Supervised TopicRank}
  \begin{frame}{Supervised TopicRank}
    \framesubtitle{TopicRank: brief overview}

    \begin{enumerate}
      \item<+->{Select the \texttt{(<NOUN>|<ADJ>)+} as candidates}
      \item<+->{Cluster candidate that ``belong to the same topic''}
      \begin{itemize}
        \item{stem overlap similarity}
      \end{itemize}
      \item<+->{Build a complete graph of topics}
      \begin{itemize}
        \item{edges weighted by a sementic strength}
      \end{itemize}
      \item<+->{Apply PageRank's ``voting concept''}
      \begin{itemize}
        \item{Important topics contribute more to the importance of the topics
              they are strongly connected to}
      \end{itemize}
      \item<+->{Extract keyphrases from the most important topics}
      \begin{itemize}
        \item{one keyphrase per topic $\rightarrow$ the first candidate in the document}
      \end{itemize}
    \end{enumerate}
  \end{frame}

  \begin{frame}{Supervised TopicRank}
    \framesubtitle{Motivations}

    \uncover<+->{TopicRank's results are encouraging:}
    \begin{itemize}
      \item<+->{Significant improvement over state-of-the-art graph-based methods}
      \item<+->{Possible improvement from 12.1\% to 30.3\% of f-score}
      \begin{itemize}
        \item{find a better strategy to identify the keyphrase of a given topic}
      \end{itemize}
    \end{itemize}~\\

    \uncover<+->{A new vision (???):}
    \begin{itemize}
      \item<+->{Combination of local importance and global likelyhood}
      \item<+->{Bigger granuarity for the importance: topic}
      \item<+->{Superised classification on smaller sets: topics}
    \end{itemize}
  \end{frame}

  \begin{frame}{Supervised TopicRank}
    \framesubtitle{Methodology}

    \begin{figure}
      \centering
      \begin{tikzpicture}[thin,
                          align=center,
                          scale=.25,
                          node distance=1.5cm,
                          every node/.style={text centered, transform shape}]
        \node[io] (document) {document};
        \node[component] (preprocessing) [below=of document] {Preprocessing};
        \node[component] (candidate_extraction) [below=of preprocessing] {Candidate Selection};
        \node[component] (topic_clustering) [below=of candidate_extraction] {Topic Clustering};
        %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
        \node[component] (preprocessing2) [right=of preprocessing] {Preprocessing};
        \node[io] (training_documents) [above=of preprocessing2] {training documents};
        \node[component] (candidate_extraction2) [below=of preprocessing2] {Candidate Selection};
        \node[component] (topic_clustering2) [below=of candidate_extraction2] {Topic Clustering};
        \node[component] (training) [below=of topic_clustering2] {Training};
        \node[io] (model) [below=of training] {classification model};
        \node[component] (candidate_classification) [below=of model] {Candidate Classification};
        \node[component] (candidate_reranking) [below=of candidate_classification] {Candidate Re-Ranking};
        \node[component] (keyphrase_selection) [below=of candidate_reranking] {Keyphrase Selection};
        \node[io] (keyphrases) [below=of keyphrase_selection] {keyphrases};
        %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
        \node[component] (topic_ranking) [left=of candidate_classification] {Topic Ranking};
        \node[above=of training_documents, yshift=-1cm] (training_label) {\huge training};
        \node[below=of topic_ranking, yshift=1cm] (topicrank) {\huge TopicRank};
        \node[right=of candidate_extraction2, xshift=-1cm] {\huge(2) Training};
        \node[right=of candidate_reranking, xshift=-1cm, yshift=.5cm] {\huge(2bis) Keyphrase Selection};
        \node[right=of candidate_reranking, xshift=-1cm, yshift=-.5cm] {\huge\textcolor{white!0}{(2bis)} + Re-Ranking};

        \path[->, thick] (training_documents) edge (preprocessing2);
        \path[->, thick] (preprocessing2) edge (candidate_extraction2);
        \path[->, thick] (candidate_extraction2) edge (topic_clustering2);
        \path[->, thick] (topic_clustering2) edge (training);
        \path[->, thick] (training) edge (model);
        \path[->, thick] (model) edge (candidate_classification);
        %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
        \path[->, thick] (document) edge (preprocessing);
        \path[->, thick] (preprocessing) edge (candidate_extraction);
        \path[->, thick] (candidate_extraction) edge (topic_clustering);
        \path[->, thick] (topic_clustering) edge node [xshift=-5.6cm] {\huge(1)} (topic_ranking);
        \path[->, thick] (topic_ranking) edge (candidate_classification);
        \path[->, thick] (candidate_classification) edge (candidate_reranking);
        \path[->, thick] (candidate_reranking) edge (keyphrase_selection);
        \path[->, thick] (keyphrase_selection) edge (keyphrases);

        \begin{pgfonlayer}{background}
          \filldraw[line width=2mm,join=round,black!10] (topic_clustering.north -| topic_clustering.east) rectangle (topic_ranking.south  -| topic_clustering.west);
          \filldraw[line width=2mm,join=round,black!10] (training_documents.north -| preprocessing2.east) rectangle (model.south  -| preprocessing2.west);
          \filldraw[line width=2mm,join=round,black!10] (candidate_classification.north -| candidate_classification.east) rectangle (keyphrase_selection.south  -| preprocessing2.west);
        \end{pgfonlayer}
      \end{tikzpicture}
    \end{figure}
  \end{frame}

  \begin{frame}{Supervised TopicRank}
    \framesubtitle{Training}

  \end{frame}

  \begin{frame}{Supervised TopicRank}
    \framesubtitle{Keyphrase selection}

  \end{frame}

  \begin{frame}{Supervised TopicRank}
    \framesubtitle{Keyphrase re-ranking}

  \end{frame}

  \begin{frame}{Supervised TopicRank}
    \framesubtitle{Example}

  \end{frame}

